import React from 'react';
import type {Node} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const ImageSlider = () => {
  return (
    <View>
      <Text>Image slider</Text>
    </View>
  );
};

export default ImageSlider;
